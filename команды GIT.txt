git status
git add --all
git commit -m "  коментарий "
git push
______________________________
git checkout -- имя__файла     -отменяем изменение в файле
git checkout -b имя_ветки      -создает ветку (от текущей ветки)и переходит на неё
git checkout master            -переходим на мастер(основная) ветку
git checkout имя_ветки         -переходим на любую созданную ранее ветку

